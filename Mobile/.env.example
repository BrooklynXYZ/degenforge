# Mobile App Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# Local Development Configuration
# ============================================
# For local development with dfx running locally or in WSL

# Local Canister IDs (from local deployment)
# Run: cd ../icp_bridge && ./deploy-and-configure.sh
# This will auto-populate these values
EXPO_PUBLIC_BTC_HANDLER_CANISTER_ID=
EXPO_PUBLIC_BRIDGE_ORCHESTRATOR_CANISTER_ID=
EXPO_PUBLIC_SOLANA_CANISTER_ID=

# WSL Configuration (if running dfx in WSL)
# Find your WSL IP: ip addr show eth0 | grep "inet " | awk '{print $2}' | cut -d/ -f1
EXPO_PUBLIC_WSL_IP=
EXPO_PUBLIC_ICP_PORT=4943

# Local ICP Host (auto-detected if WSL_IP is set)
# Leave empty to use auto-detection based on WSL_IP
EXPO_PUBLIC_ICP_HOST=

# ============================================
# Production Configuration
# ============================================
# For production builds, use .env.production instead
# Production canister IDs are set automatically by deploy-mainnet.sh
# Or set them manually after deploying to ICP mainnet

# Production ICP Host (used when __DEV__ is false)
# Defaults to https://icp-api.io if not set
# EXPO_PUBLIC_ICP_HOST=https://icp-api.io

# ============================================
# Notes
# ============================================
# - Local development: Use .env with local canister IDs
# - Production builds: Use .env.production with mainnet canister IDs
# - The app automatically selects the correct host based on __DEV__ flag
# - Never commit .env or .env.production files to git
