import { View } from 'react-native';

// This is a placeholder index route that Expo Router needs
// The actual app navigation is handled by AppNavigator in _layout.tsx
export default function Index() {
  return <View />;
}

